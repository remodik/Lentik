fastapi==0.135.1
uvicorn[standard]==0.41.0
sqlalchemy[asyncio]==2.0.48
asyncpg==0.31.0
psycopg2-binary==2.9.10
alembic==1.18.4
pydantic==2.12.5
python-multipart==0.0.22
passlib==1.7.4
python-jose[cryptography]==3.5.0
aiofiles==25.1.0
pydantic-settings==2.13.1

# ── Масштабирование (опционально, нужны только при включении функции) ─────────
# Redis: общий WS fan-out и общие rate-лимитеры. Нужен, если задан
# REDIS_URL. Импортируется лениво — без REDIS_URL пакет не требуется.
redis==5.2.1
# S3-совместимое хранилище загрузок. Нужен при STORAGE_BACKEND=s3.
aioboto3==13.2.0
# Web Push (VAPID) — уведомления вне приложения. Нужен, только если заданы
# VAPID_PUBLIC_KEY/VAPID_PRIVATE_KEY. Импортируется лениво. Раскомментируйте:
# pywebpush==2.0.3

# ── Тесты (dev) ──────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
